#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void ctvorenamtric(int r, int c, int matrix[][c]) {
    srand(time(NULL));
for (int i = 0; i < r; i++) {
      for (int j = 0; j < c; j++) {
       matrix[i][j] = rand() % 201 - 100;
        }
    }
}

void nahodjena(int r, int c, int matrix[][c], int minimalnayelementy[]) {
    for (int i = 0; i < r; i++) { int minimalnaya = matrix[i][0];
    for (int j = 1; j < c; j++) {
      if (matrix[i][j] < minimalnaya) {
            minimalnaya = matrix[i][j];
            }
        }
        minimalnayelementy[i] = minimalnaya;
    }
}

void print_matrix(int r, int c, int matrix[][c]) {
    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            printf("%d\t", matrix[i][j]);
        }
        printf("\n");  }
}

void print_array(int size, int array[]) {
    for (int i = 0; i < size; i++) {   printf("%d\t", array[i]);
    } printf("\n");
}
int main() {
    int r, c;
    printf("Ведіть(вертикальні стовпці)");
    scanf("%d", &r);
    printf("Ведіть (горизонталь стовпці)  ");
    scanf("%d", &c);
    int matrix[r][c];
    int minimalnaya_elements[r];
    ctvorenamtric(r, c, matrix);
    printf("\nЗгенерована матриця:\n");
    print_matrix(r, c, matrix);
    nahodjena(r, c, matrix, minimalnaya_elements);
    printf("\nМінімальние елементи :\n");
    print_array(r, minimalnaya_elements);
    return 0;
}
